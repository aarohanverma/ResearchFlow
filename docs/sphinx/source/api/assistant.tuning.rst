assistant — tuning constants
==============================

Centralised tuning knobs (thresholds, cooldowns, retry counts) used by
the orchestrator + middleware chain. Keeps tweakable numbers in one
place so ops can lower a threshold without grepping the codebase.

.. automodule:: app.assistant.tuning
   :members:
   :undoc-members:
   :show-inheritance:
   :special-members: __init__
