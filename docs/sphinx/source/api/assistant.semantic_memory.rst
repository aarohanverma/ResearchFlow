assistant — semantic memory
=============================

Vector + lexical retrieval over the user's accumulated session memory
entries. Surfaces semantically related prior facts/preferences back into
the planner's context window when the current query overlaps with them.

.. automodule:: app.assistant.semantic_memory
   :members:
   :undoc-members:
   :show-inheritance:
   :special-members: __init__
